{
    "buildCommand": "pnpm build && pnpm --filter \"application\" build",
    "outputDirectory": "application/dist",
    "framework": "vite",
    "rewrites": [
      { "source": "/(.*)", "destination": "/index.html" }
    ]
  }