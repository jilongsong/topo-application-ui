{
    "buildCommand": "pnpm build && pnpm --filter \"playground\" build",
    "outputDirectory": "application/dist",
    "framework": "vite",
    "rewrites": [
      { "source": "/(.*)", "destination": "/index.html" }
    ]
  }